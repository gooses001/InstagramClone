<!DOCTYPE html>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

<html>
  <head>
    <title>TweetApp</title>
  </head>
  <h1>ブログ一覧</h1>
  <p><%= notice %></p>
  <table>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
    </tr>
  
  <% @blogs.each do |blog| %>
    <tr>
      <td><%= blog.title %></td>
      <td><%= blog.content %></td>
      <td class="alert alert-info"><%= link_to'詳細を確認する',blog_path(blog.id) %></td>
      <td class="alert alert-warning"><%= link_to "ブログを編集する", edit_blog_path(blog.id),data: { confirm: '本当に編集していいですか？' } %> </td>
      <td class="alert alert-danger"><%= link_to 'ブログを削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %> </td>
    </tr>
  <% end %>

  </table>

  <span class="label label-default">
    <%= link_to '新しくブログを投稿する', new_blog_path %>
  </span>


</html>